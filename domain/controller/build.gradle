dependencies {
    compile project(':domain-aif')
    testImplementation 'org.mockito:mockito-inline:2.13.0'
    compile('com.fasterxml.jackson.core:jackson-databind:2.0.1')

    compile('org.springframework.boot:spring-boot-starter-webflux')
}